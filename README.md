# My Flutter To-Do List App

A sleek, modern, and functional To-Do List application built with Flutter and Dart. This app allows users to easily track, add, and manage their daily tasks with a clean and intuitive user interface.

## ✨ Features

* **Task Management:** Add new tasks easily via a dedicated input field.
* **Completion Tracking:** Mark tasks as complete using the checkbox.
* **Deletion:** Permanently remove tasks with a dedicated trash icon button.
* **Responsive UI:** Designed to work well across both mobile (iOS/Android) and desktop platforms.
* **Modern Design:** Features a subtle card elevation and a distinctive color palette (as seen in the provided UI screenshot).

## 📸 Screenshot

| Home Screen |
| :---: |
| [Insert your image here, or link to the uploaded image] |
| *Example UI showing the task input and list view* |

## 🛠️ Technologies Used

* **Framework:** Flutter (3.x or newer)
* **Language:** Dart
* **UI Components:** `StatefulWidget`, `ListView.builder`, `Card`, `TextFormField`, and custom `TaskTile` widget for task representation.

## 🚀 Getting Started

Follow these instructions to get a copy of the project up and running on your local machine.

### Prerequisites

You need to have the Flutter SDK installed and configured on your system.

* [Flutter Installation Guide](https://flutter.dev/docs/get-started/install)
* Dart 2.x or later

### Installation

1.  **Clone the repository** (if hosted on Git):
    ```bash
    git clone [your-repo-link]
    cd my_flutter_todo_list
    ```

2.  **Get the project dependencies:**
    *(This app uses only core Flutter dependencies, so typically this step is just for confirmation.)*
    ```bash
    flutter pub get
    ```

3.  **Run the application:**
    ```bash
    flutter run
    ```
    Alternatively, open the project in your preferred IDE (VS Code or Android Studio) and launch it on a connected device or emulator/simulator.

## 📁 Project Structure

The core functionality is contained within a single file for simplicity:

Output:
# My Flutter To-Do List App

A sleek, modern, and functional To-Do List application built with Flutter and Dart. This app allows users to easily track, add, and manage their daily tasks with a clean and intuitive user interface.

## ✨ Features

* **Task Management:** Add new tasks easily via a dedicated input field.
* **Completion Tracking:** Mark tasks as complete using the checkbox.
* **Deletion:** Permanently remove tasks with a dedicated trash icon button.
* **Responsive UI:** Designed to work well across both mobile (iOS/Android) and desktop platforms.
* **Modern Design:** Features a subtle card elevation and a distinctive color palette (as seen in the provided UI screenshot).

## 📸 Screenshot

| Home Screen |
| :---: |
| [Insert your image here, or link to the uploaded image] |
| *Example UI showing the task input and list view* |

## 🛠️ Technologies Used

* **Framework:** Flutter (3.x or newer)
* **Language:** Dart
* **UI Components:** `StatefulWidget`, `ListView.builder`, `Card`, `TextFormField`, and custom `TaskTile` widget for task representation.

## 🚀 Getting Started

Follow these instructions to get a copy of the project up and running on your local machine.

### Prerequisites

You need to have the Flutter SDK installed and configured on your system.

* [Flutter Installation Guide](https://flutter.dev/docs/get-started/install)
* Dart 2.x or later

### Installation

1.  **Clone the repository** (if hosted on Git):
    ```bash
    git clone [your-repo-link]
    cd my_flutter_todo_list
    ```

2.  **Get the project dependencies:**
    *(This app uses only core Flutter dependencies, so typically this step is just for confirmation.)*
    ```bash
    flutter pub get
    ```

3.  **Run the application:**
    ```bash
    flutter run
    ```
    Alternatively, open the project in your preferred IDE (VS Code or Android Studio) and launch it on a connected device or emulator/simulator.

## 📁 Project Structure
The core functionality is contained within a single file for simplicity:

output:
<img width="927" height="719" alt="image" src="https://github.com/user-attachments/assets/2f23aee3-2854-41a2-a46d-532053dbbda1" />
